import {
  
} from '../actionTypes'

const initialState = {
  
}

export default function showsReducer(state = initialState, action) {
  switch(action.type) {
    
    default:
      return state
  }
}